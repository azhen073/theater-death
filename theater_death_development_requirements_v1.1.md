# 《剧院死神》在线法官 · 开发需求文档

版本：v2.0.1-beta · 整合工作版（并入 S3 裁定；v1.2 增补：观战；v1.3 增补：房主踢人；v1.4 增补：实验模式板子编辑器；v1.5 增补：终局退出；v1.6 变更：语音媒体服务替换为声网；v1.7 变更：天理夜死移交时机对齐 + 规则 2.0 命名预设；v1.8 增补：账号体系与 v2 服务端/前端体系准入；v1.9 整理：文档一致性核对（无玩法变更）；v2.0.1-beta 变更：房间解散任意阶段生效 + 遗弃房间 24 小时回收，见文末版本记录）  
适用读者：给 Codex 与项目维护者  
整理日期：2026-09-16 · 增补记录 2026-09-17 起 · 最近整理 2026-09-20（v2.0.1-beta）

> **交付目标**  
> 实现一套服务端裁定的在线游戏，默认 13 人、9 种身份；支持可验证的板子配置、文字交流、公共语音控制、刷新恢复、日志与终局复盘。同一套代码支持玩家电脑托管和第三方服务器托管。

依据：《玩家规则书 v1.1》R-01–R-54、原始文档及三轮用户裁定。玩法以最新用户确认内容为准；本文件标为“实现约定”的内容是可替换的工程方案，不是额外角色能力。Q-01–Q-08 已全量定值（见规则书第 09 章），正式板子封版；后续变更走版本记录。

## 01  首版范围与验收边界

| 范围 | 首版要求 |
| --- | --- |
| 必须交付 | 房间 / 昵称 / 准备；随机席位和身份；9 种角色；双阶段；行动、投票、胜负；权限；文字房；公共语音许可；三类日志；复盘；刷新 / 短暂断线恢复。 |
| 必须预留 | 角色参数、角色数量、胜利与阶段策略可配置；规则快照冻结；同一程序两种托管方式；媒体服务替换与文字降级。 |
| 不承诺 | 服务端崩溃恢复、休眠防护、故障迁移；公开匹配、专门多设备管理、录音、语音转写、通用桌游编辑器。**观战自 v1.2 起以“绑定玩家只读第二屏”形式纳入（见 §07），仅此一种形态；自由摄像机/上帝视角观战仍不承诺。账号体系自 v1.8 起纳入交付范围（注册 / 登录 / 管理后台，见文末版本记录）。** |
| 信任边界 | 朋友局信任房主不读底层秘密；正常管理员界面局内不剧透。第三方部署不是自动获得第三方不可读数据或陌生人平台安全认证。 |
| 设备与入口 | 仅 remote 产品模式；电脑优先、手机可用；目标浏览器 Chromium 系与 Safari。所有现场玩家也可使用远程入口。 |

首版容量目标为单个十几人的房间，不顺带承诺多房间高并发。16GB 内存笔记本作为测试基线是容量目标，不是已完成压测的结论；规则服务器不需要独立显卡或在线大模型。语音媒体负载单独验收。

> **故障后果须公开**  
> 不做服务器恢复，意味着后端崩溃、关机或休眠可能终止整局。不得把“已写日志”包装成“能恢复运行”。玩家刷新恢复是另一项必须完成的功能。

### 文档与来源优先级

最新用户裁定 > 之前用户裁定 > 原板未修改条文。旧两份助手简报只作历史参考。若发现冲突，记录 Q 编号及受影响测试；不得让 AI 在真实对局中临场补判。

<!-- pagebreak -->

## 02  功能清单与模块划分

| ID | 需求 | 验收判据 |
| --- | --- | --- |
| F-01 | 建房、加入、昵称、准备 | 达到配置人数才分配席位与角色；不可重复占座或重新抽牌。 |
| F-02 | 确定性规则引擎 | 同一配置、初始状态和合法动作序列得到同一状态与事件。 |
| F-03 | 行动 / 投票 / 计时 | 确认、空行动、超时默认值正确；不因网络重试多执行。 |
| F-04 | 按身份和阶段授权 | 响应、推送、历史查询与语音都受相同权限规则控制。 |
| F-05 | 交流 | 公屏白天活人可写；永久阵营文字房；死亡成员只读；夜间全体静音。 |
| F-06 | 刷新与重连 | 恢复原玩家及当前授权视图；不重抽、不加时间、不恢复过期操作。 |
| F-07 | 日志与天理复盘 | 公共、个人、服务端完整日志；终局公开身份 / 状态 / 胜负解释。 |
| F-08 | 配置与开局自检 | 非法板子 / 必填裁定缺失时拒绝正式开局并解释原因。 |
| F-09 | 两种部署 | 同一构建产物通过配置部署在玩家电脑或第三方服务器。 |
| F-10 | 可执行测试 | 规则、权限、异常网络、浏览器、模拟对局都有脚本与真实报告。 |

### 建议技术栈（实现约定）

TypeScript 统一前后端数据结构；Node.js 运行后端；Socket.IO 处理实时动作、聊天与状态；SQLite 保存日志和必要数据；网页采用 React + Vite；测试使用 Vitest 与 Playwright。媒体通过独立 VoiceAdapter 接入现成 WebRTC 服务。全部组件以 Docker 镜像构筑与运行（见 §12）。不要自行发明底层网络协议。

```text
engine/       纯规则、角色、事件、阶段与胜负
visibility/   玩家视图、消息历史与权限决策
server/       会话、房间、命令、计时、日志存储
web/          电脑与手机网页
voice/        公共语音的服务端权限适配层
rulesets/     板子配置、版本与验证器
deploy/       Dockerfile、compose 编排与运行手册
tests/        规则、权限、协议、浏览器及模拟对局
```

规则引擎不得依赖浏览器、网络连接或当前真实时间。服务端传入动作和时钟事件，引擎返回新状态与事件；网络层负责授权投影和发送。SQLite / Socket.IO 等是建议实现，不与角色机制绑定。

<!-- pagebreak -->

## 03  核心数据模型

| 对象 | 至少包含 | 禁止混淆 |
| --- | --- | --- |
| Game | gameId、规则版本与快照、日夜编号、阶段、入夜阶段、流程步骤、天理持有者、结果 | 游戏阶段不等于当前行动步骤。 |
| Player | 稳定 playerId、座位、昵称、角色、阵营、屠边分组 | 昵称、席位、连接编号都不是认证凭证。 |
| PlayerState | life、revealed、各技能次数、守护历史、投票资格（含禁投与天理票权冻结） | alive / dying / dead 与翻牌状态独立。 |
| Connection | playerId、在线状态、当前连接 | 掉线不等于死亡。 |
| NightContext | 合法攻击事件、守护、主致死事件、名单快照、最终死亡集合 | 真实状态与玩家可见死讯不同。 |
| ActionWindow | 窗口 ID、资格快照、上限、截止时间、确认方案与版本 | 一个队伍的攻击池不是每个成员各自一池。 |
| ChatMembership | roomId、playerId、joinedAt、historyPolicy、读写权限 | 能读房间不等于能发送或共同决策。 |
| Event | 事件 ID、内部顺序、日夜 / 步骤、类型、载荷、可见范围 | 服务端事件不可直接广播全体。 |

### 身份标签必须拆分

```text
roleId             具体身份，例如 death / spirit / mourner
factionId          human / death_faction
eliminationGroup   deity / civilian / researcher / none
countsForHumanWin  丧亲者为 false；死神、魂灵为 true
knownIdentities    按角色独立生成，不能直接返回阵营全名单
```

丧亲者：阵营查验为死神阵营；roleId 不是死神、不是魂灵。死神初始只能获得规定的魂灵身份知识，不能获得丧亲者；丧亲者知道魂灵，不知道死神。魂灵初始互认。未来重复特殊角色的互知关系必须另定义，不能外推。

### 游戏分配与随机

使用服务端随机分配，保留最终分配结果；测试允许固定种子复现。分配种子和全身份表局内不公开。会话凭证使用不可预测值，不复用可预测的测试种子、房间码或座位号。无需引入可验证洗牌协议。

<!-- pagebreak -->

## 04  攻击事件与唯一主死因

对应 R-07、R-17–R-19、R-28、R-32。一次“刀”必须是一个独立、已确认且合法的攻击事件；多人确认同一个团队方案只产生该方案指定的刀数。保留所有攻击事实，同时只认一个当前死亡的主死因。

```text
AttackEvent:
  eventId, nightId, sourceActionId, sourceRoleId
  targetPlayerId, orderKey, blocked

FatalRecord:
  playerId, nightId, primaryFatalEventId
  primaryCauseKind   # direct_attack / guard_sacrifice / vote ...
  preventedByRescue, committedAsDeath
```

实现约定：先按已冻结的规则次序排列全部效果，再执行；绝不按包到达顺序归因。袭击批次内部次序按规则书 R-48 定值（目标座位升序；同目标来源优先级：魂灵 → 死神 → 莱莱可），主死因裁决据此执行。

### 结算算法约束

守护建立每个目标至多一刀的防护。第一次可挡攻击消耗防护并记录成功；下一次有效攻击令目标濒死，记录首个主致死事件。已经濒死时，后续攻击仍记录，但不覆盖主致死事件。

所有直接攻击处理完毕后，按“两个不同守护对象都实际受到直接攻击”判断守护牺牲。一阶段若满足条件，向门先生应用牺牲事件；若他此前已被攻击致死，只记录后续事件而不覆盖主死因。

还魂曲是“阻止目标本次夜间死亡”，不是第二个护盾，也不是先提交 Death 再 Revive。若救人成功，应取消本次有效死亡承诺；同夜已处理的攻击 / 双守牺牲不得因重新扫描而再次触发，导致被救者重新死亡。

> **主死因与完整日志**  
> 玩家规则只认首个有效主死因；开发日志仍可保留被挡攻击、后续补刀和未取得主死因资格的牺牲。两者不是二选一。门先生回归只查当前有效死亡记录，不查“历史曾因守护死过”。

### 失技统计

```text
T = set(本夜死神与魂灵合法执行的袭击目标)
D = set(还魂曲之后、本夜确认死亡的玩家)
N = 本局开局魂灵人数

if nightStage == STAGE_1 and size(T ∩ D) > N:
    death.stage1AttackDisabled = true
```

D 不使用“最终一刀属于死神阵营”筛选；多人多刀同一目标只计一个人。二阶段忽略一阶段禁用标记，使用独立联合行动额度。阶段一不会被晨间阶段二覆盖重算。

<!-- pagebreak -->

## 05  夜间窗口与角色执行

| 窗口 / 步骤 | 行为 | 实现约束 |
| --- | --- | --- |
| 守护 45s | 门先生 0–2 目标 | 自选禁止；校验同对连续两晚、单人连续三晚。 |
| 阵营 90s | 一阶段死神与魂灵分开协商 / 操作；二阶段联合 | 建议一阶段并行独立窗口共享时段，彼此方案不可见；攻击固定次序按 R-48。 |
| 刺杀 45s | 莱莱可按入夜阶段与翻牌分支行动 | 濒死仍可执行；一次性次数不因被杀或刷新重置。 |
| 牺牲判定 | 所有直接攻击之后判双守 | 不会覆盖已有攻击主死因；不重复执行。 |
| 查验 45s | 降临者查魂灵或阵营 | 仅返回所问类别；附带视野按 R-14 / R-51（二阶段无名单）。 |
| 还魂曲 45s | 一阶段水妖名单与救人 | 只能名单内自己以外者；使用后不再提供一阶段名单。 |
| 夜末确认 | 提交实际死亡记录 | 濒死统一确认；一阶段水妖未被救则死亡。 |
| 回归选择 45s | 二阶段夜死水妖选择已死者 | 自己禁止；白天死亡 / 一阶段末夜死亡均不开放。 |

魂灵一阶段额度取本夜行动资格快照中的存活魂灵数；濒死保留本夜资格，不因后续网络刷新减少额度。二阶段死神 / 魂灵共享最多 2 刀，不叠加一阶段额度。死神一阶段失技仍不能读魂灵私聊。

### 90 秒团队方案：规则与界面分离

实现方案草稿、方案版本、确认回执；死亡成员只有聊天读取权，不得编辑草稿、投同意票或确认攻击。团队拍板机制按规则书 R-47 实现：版本化草稿 + 全员确认，截止时取最新全员确认版本，从未达成则空刀；“最后点击者获胜”等被否决的默认策略不得出现。

### 防止流程节奏泄露

实现要求：公开界面不得展示“某身份正在行动”“某人已濒死”“技能刚刚消耗”等隐藏信息。建议保留固定夜间时段 / 填充窗口，不因隐藏角色死亡、失技或提前确认而让全体观察到可识别的时长变化。该计时展示方案是工程建议；公开时钟与角色窗口应分开设计。

对不具备视野的濒死玩家，页面不能提前变灰、禁用本夜合法行动或返回“你已被杀”。此前已死者不重新获得普通夜间资格；水妖的死亡选择是明确的例外窗口。

<!-- pagebreak -->

## 06  白天结算、阶段与胜利

### 一个检查点必须原子完成

这里的“原子”指结算内部不能插入新的玩家命令，也不能在被动效果之间提前宣布赢家。允许生成多条有序公告，但状态与效果处理完成后，统一发布一致的授权结果。

```text
晨间：
  夜间死亡确认
  → 二阶段水妖选择已经完成
  → 执行水妖晨间回归
  → 生成死亡 / 回归公告
  → 翻牌、科研员公告
  → 判断阶段变化并执行
  → 门先生依当前主死因立即回归
  → 完成本节点其余已定义效果
  → 唯一一次胜负检查

投票后：
  按配置结票 → 出局与票型公示
  → 翻牌、阶段变化、门先生回归
  → 唯一一次胜负检查（不触发水妖复活）
```

阶段只允许 1→2，不逆转。以入夜阶段决定水妖死亡技能，不能看到晨间 stage=2 就补发窗口。一次性效果按效果 ID / 消耗标记去重，不能循环扫描死亡状态重复触发。

### 默认 13 人胜利策略

```text
deathWins = researcher.currentLife == DEAD and (
    allConfiguredDeitiesAreDead or allConfiguredCiviliansAreDead
)
humanWins = allConfiguredDeathAndSpiritPlayersAreDead

if deathWins: return DEATH_FACTION
if humanWins: return HUMAN
return ONGOING
```

上面只表达默认板，不是允许空分组的通用代码。检查对象应来自本局配置分组，不是固定座位。科研员回归使 deathWins 的前置条件为假；丧亲者不阻止 humanWins。不得新增“人数相等即胜利”等传统狼人杀条件。

投票基础资格必须同时考虑当前生命状态与角色投票限制：死者不能投票，莱莱可翻牌禁投期间不得参加任何投票（含竞选与放逐），天理票权同步冻结；二阶段恢复投票权后同步恢复（R-44）。

### 白天流程实现依据

白天时序、竞选、发言轮、放逐投票、平票、遗言、票型公示与天理移交均按规则书 R-41–R-46 定值实现，不得自行改用某线下版本细则。天理票权 1.5 以整数定点结算（普通票 2 单位、天理票 3 单位）。

<!-- pagebreak -->

## 07  权限与信息投影

F-04 对应 R-10、R-14、R-24、R-27、R-31、R-34–R-40。服务端先按接收者裁剪，再发送；不能先发全量状态，再用页面隐藏。连接成功不代表有权执行所有消息，应逐条授权与校验。[T5]

| 信息 / 操作 | 普通活人 | 普通死者 | 阵营房死者 | 管理员（局内） |
| --- | --- | --- | --- | --- |
| 公共公告 / 白天聊天读 | 允许 | 允许 | 允许 | 按自身玩家视图 |
| 白天公屏发送 | 允许 | 禁止 | 禁止 | 不额外授权 |
| 夜间公屏发送 | 禁止 | 禁止 | 禁止 | 不额外授权 |
| 阵营房读取 | 仅成员 | 非成员禁止 | 允许 | 按自身成员关系 |
| 阵营房写 / 协同攻击 | 仅存活合格成员 | 禁止 | 禁止 | 不额外授权 |
| 名单 / 查验 / 私有行动 | 角色授权范围内 | 默认不新增；死亡选择例外 | 房间读取不含全量技能数据 | 按自身角色 |
| 全身份 / 完整服务端日志 | 终局前禁止 | 终局前禁止 | 终局前禁止 | 终局前界面禁止 |

### 旁观者（v1.2 增补）

观战 = **绑定一名玩家的只读第二屏**，不是独立视角：

- 一名观众绑定一名玩家，双向上限 1:1（每名玩家最多一名观众）；需昵称；大厅期、对局中、终局后均可加入（终局后观战用于复盘）。
- 可见范围 = **所绑定玩家的完整视图**（含该玩家身份与私有信息）——这是产品设计上的信任模型；观众不得因此获得任何其他玩家的私有信息或全量状态。
- 禁止项：一切游戏提交（行动 / 投票 / 竞选 / 发言结束等）、公屏与阵营房**写**、语音**发布**；语音仅可旁听（canPublish=false）。观众不占玩家席位、不计入开局人数与胜负。
- 观众跟随绑定玩家的公屏与阵营房**读**权限（如绑定死神/魂灵则可读阵营房，非成员则不可）。
- 会话为独立观战会话；同一浏览器“玩家 + 观战”互斥（单会话 cookie）。
- 观众计入媒体服务用量（声网免费层按「人 × 分钟」计费；媒体服务已于 v1.6 由 LiveKit 替换为声网）。
- 房主管理：房主可在成员/观众列表移出观战者（不限阶段；被移出者若在语音则一并移除媒体参与者）。

### 阵营成员与历史

初始魂灵入房；死神仅在阶段转换且存在未淘汰魂灵时加入；丧亲者永不加入。房间跨白天存在，存活成员白天也能发送。死亡不踢出房间，但彻底撤销写与协作权限；回归后恢复当前应有权限。

死神入房按 R-52 执行：转阶段结算时点一次判定；已死则只读加入；只发布加入后消息，不补拉、不含加入前历史。存储 joinedAt / joinedEvent，不得把“已死成员能读”扩大成“任何死亡阵营成员都可入房”。

### 错误、历史与侧信道

可以解释“超过你的目标上限”“该窗口已结束”“不能连续三晚守护同一人”。不能解释“目标被保护”“目标是水妖所以不合法”等未授权秘密。名单外目标错误只说明不在本次可选范围。

私密历史通过当前身份与事件授权读取，不直接信任客户端传来的 playerId / roomId / 日志游标。公共与个人事件流使用独立的对外游标；避免公开全局连续事件号、隐藏事件计数或全量状态版本差异，从而泄露夜间活动数量。

不开发可绕过死亡禁言的编辑、表情、输入中提示等附加功能；或对它们施加同样的写权限。正式前端日志、错误堆栈与调试工具不得包含全身份和夜间目标。

<!-- pagebreak -->

## 08  命令协议、确认与时间

使用 HTTPS 获取页面、加入 / 恢复会话；实时通道传递操作意图和授权事件。Socket.IO 默认并不保证每条消息送达，确认、重试与状态同步必须由应用补齐。[T1]

```text
// 客户端提交意图；身份由服务端会话解析，不能信任载荷自报。
{
  "gameId": "g_...",
  "windowId": "night_2_assassination",
  "requestId": "不可重复的请求编号",
  "action": "CONFIRM_ABILITY",
  "proposalRevision": 3,
  "targets": ["player_6"]
}

// 回执：不返回其他人的秘密状态。
{
  "requestId": "同一编号",
  "status": "accepted",
  "confirmationId": "c_...",
  "confirmationStatus": "confirmed"
}
```

实现约定：服务端验证会话、所属游戏、窗口、资格、配额、目标、技能余量和确认版本；在串行房间命令队列中提交。幂等键可用 gameId + actorId + requestId；窗口级“只生效一次”另有唯一约束，不能只靠请求编号防双击。

确认前为草稿，不产生攻击。主动空行动也要确认。确认后的修改按 R-47 执行：编辑产生新版本并需重新全员确认，旧版本的全员确认在截止时作为兜底；同一时间只有一份最终有效方案，重复确认幂等返回原回执，不得重复扣次数。非法请求和没有执行技能的空行动不应消耗一次性技能，此为明确标记的实现建议。

### 时限与断线

| 场景 | 规则 |
| --- | --- |
| 死神阵营窗口 | 90 秒；截止时取最新全员确认版本（R-47），从未达成则空刀。 |
| 其他角色选择 | 45 秒，包括有资格的水妖死亡选择。 |
| 投票 / 发言 | 各 60 秒上限；票超时弃票，发言超时结束或放弃。 |
| 掉线未提交 | 不暂停；超时按空行动 / 弃票处理。 |
| 截止后补包 | 拒绝，不补发上一回合行动；刷新不改变截止时间。 |

真正截止由服务器时钟决定；页面只显示剩余时间。记录主动放弃、未提交超时、断线超时三种原因。网络慢不是允许客户端自行延长的理由；重复发送同一已接受请求，应返回原回执而非再次执行。

<!-- pagebreak -->

## 09  会话恢复与三类日志

### 刷新 / 短暂断网恢复

首次加入时签发独立的会话凭证，关联稳定 playerId。不要把 Socket.IO 的临时连接编号当身份；其官方说明建议独立会话 ID。[T2] 本项目建议同源部署并使用安全 Cookie 管理凭证，具体保护属性随部署方案配置。

```text
加入 → 签发凭证 → 绑定 playerId → 浏览器保留凭证
刷新 / 重连 → 校验凭证 → 查询原席位
  → 重新计算当前权限
  → 返回个人状态、当前允许的操作、截止时间、确认结果
  → 返回允许读取的公共 / 个人 / 阵营历史
  → 恢复实时订阅
```

Socket.IO 的连接状态恢复不能保证每次成功，因此应用必须具备授权快照重载路径。[T3] 恢复后不能直接复用断线前的旧写权限或旧语音许可；不要自动补发用户无权知道的历史。

不专门做多设备管理，但两个标签页必须映射同一玩家，不增加席位、票数、刀数或技能次数。清空凭证后的人工认领不是已承诺功能，不得仅凭昵称让其他人接管身份。

### 三套日志是三种视图，不必是三份重复事实

| 视图 | 内容 | 可见性 |
| --- | --- | --- |
| 服务端完整事件 | 配置快照、身份分配、确认动作、攻击 / 守护、主死因、救人 / 回归、查验、阶段、票型、胜负、错误 / 管理事件 | 局内不开放完整客户端入口；密钥 / 会话凭证不入游戏日志。 |
| 个人事件 | 本人已合法获得的身份、知识、查验、技能结果、确认回执 | 仅对应玩家；刷新按授权恢复。 |
| 公共事件 | 白天文字、死亡 / 回归公告、允许的翻牌公告、票型、阶段与终局结果 | 玩家均可读取；发送权限另算。 |

聊天频道按自身成员权限存储和查询。服务端内部事件、游戏复盘事件、调试日志应分开；“完整复盘”不等于向浏览器导出数据库。赛后按 R-53 公开：全部交流（公屏、阵营房，含加入前历史）、行动日志（查验、夜间行动、守护与还魂）、全身份、最终生命状态、胜负说明与每日时间线；不得混入密钥、凭证与调试数据。

持久化目标是保留日志并支持当前在线服务的历史读取，不承诺由这些日志恢复崩溃的引擎进程。建议状态变更与事件写入一致提交；写盘失败明确报错，不能提示已成功却悄悄丢失记录。

<!-- pagebreak -->

## 10  公共语音与网页体验

本版不再要求夜间临时阵营语音房。阵营协商使用永久文字房；只有公共白天语音。夜间全体静音，白天只有获得发言许可的活人可开麦，死者仅公共旁听。开麦子阶段按 R-43：竞选候选发言轮、发言轮中的当前发言者、遗言者；投票期间全体禁麦。

### 语音接入要求

浏览器访问麦克风需要用户许可和安全上下文，不能无条件自动开麦。[T4] 页面提供“加入语音 / 授权”“静音”“设备选择”“文字模式”及清楚的状态反馈。已授权后，流程切换只授予发言资格，不强迫取消玩家自己的静音。

实际许可必须由媒体服务或可信服务端实施，不能只把客户端麦克风图标置灰。夜间禁止所有发送者发布音频；死亡、发言结束、断线重连都重新校验发言资格。若所选服务只能做前端静音，不算满足硬性权限验收。

语音失败不改变胜负，不暂停计时。白天公屏继续可用；夜间只有获准阵营房可文字协商，不能为降级而开放全体夜间公屏。未完成自动语音控制时，应明确标记“文字测试模式”，不能声称全功能已完成。

不录音、不转写；无视频需求。语音提供商、费用和具体部署方案尚未指定，禁止自动开通付费资源。媒体连接需要独立验证，网页隧道连通不等于媒体已经可用。

### 页面清单与可用性

| 页面 | 必须呈现 |
| --- | --- |
| 加入 / 大厅 | 邀请入口、昵称、准备状态、人数、自检、规则版本与板子摘要。 |
| 个人身份 | 自己的身份 / 技能 / 合法已知对象；不出现完整身份表。 |
| 对局主屏 | 公共席位状态、公告、合法操作、目标选择、明确确认 / 放弃、倒计时、公屏。 |
| 阵营房 | 仅授权成员可见；死亡只读；攻击协作与聊天权限分开。 |
| 语音条 | 可否发言、是否本地静音、连接 / 权限问题、文字降级。 |
| 复盘 | 结果、原因、全身份与最终状态、按天夜整理的授权复盘时间线。 |

电脑优先，手机基本操作不能依赖悬停或复杂拖动。座位号与昵称同时显示，目标确认再次呈现座位。颜色不是唯一提示；按钮需有文字。错误解释自身操作限制，不泄露隐藏局面。

<!-- pagebreak -->

## 11  规则配置与开局验证

角色机制写成可测试函数，目标上限、技能次数、人数、时限与胜利 / 阶段策略独立配置。新机制仍需代码，不要求万能规则解释器。开局保存完整配置快照与版本，局中修改只影响下一局。

```text
// 配置示意，不是可直接开局的最终配置。
{
  "rulesetId": "theater-death",
  "version": "1.1",
  "roles": {
    "laike": 1, "door": 1, "water": 1, "descender": 1,
    "researcher": 1, "civilian": 4,
    "death": 1, "spirit": 2, "mourner": 1
  },
  "timersSeconds": {"faction": 90, "ability": 45,
                    "vote": 60, "speech": 60, "election": 30,
                    "handover": 45, "lastWords": 60},
  "sheriff": {"enabled": true, "voteWeight": 1.5,
              "handover": "designate_or_destroy"},
  "lastWords": {"firstNight": true, "dayEliminated": true,
                "otherNights": false},
  "teamConfirm": "unanimous_by_revision",
  "duplicateTargetPolicy": "allow",
  "attackOrder": "seat_asc_then_source_priority",
  "researcherAnnouncement": {"count": "alive_at_announcement",
                             "includesMourner": true},
  "stageTriggerSnapshot": "death_event",
  "replayDisclosure": "all_chat_and_action_log",
  "stage1SpiritQuota": "eligibleLivingSpiritCount",
  "stage1DeathQuota": 2,
  "stage1OverkillThreshold": "initialSpiritCount",
  "stage2JointQuota": 2,
  "guardTargets": 2,
  "guardBlocksPerTarget": 1,
  "simultaneousWinPriority": "death_faction",
  "researcherCondition": "currently_dead"
}
```

### 验证器必须拒绝的情形

人数不符；角色数量为负或无定义；技能引用缺失角色；空神职 / 空平民触发“全灭”；零魂灵意外使开局转阶段；无科研员却保留其死亡胜利前置；未支持的特殊角色重复。不能把空集合 every() 的结果当成人工确认的规则。

正式模式仅使用默认 13 人命名预设（R-54）。实验模式可运行未支持配置，但大厅须醒目标记并保存实验值，不得宣布任意人数 / 任意组合都可用。

### 开局自检

检查配置、人数、角色 / 座位一致性、准备状态、日志目录可写、实时连接；启用语音时检查基础授权与媒体连接状态。自检不能证明未来不掉线、所有人听得清或陌生人安全，实际听说测试仍由玩家参与。

<!-- pagebreak -->

## 12  两种部署与运维边界

| 项目 | 玩家电脑托管 | 第三方服务器托管 |
| --- | --- | --- |
| 程序 | 同一服务端和网页构建 | 同一服务端和网页构建 |
| 容器 | 同一 Docker 镜像与 compose 编排 | 同一 Docker 镜像与 compose 编排 |
| 公网入口 | HTTPS 隧道或等价入口 | HTTPS 反向代理 / 托管入口 |
| 数据位置 | 本机专用数据目录 | 服务器专用持久数据目录 |
| 运行依赖 | 房主保持后端及入口运行 | 托管环境保持服务运行 |
| 媒体 | 通过同一 VoiceAdapter 配置 | 通过同一 VoiceAdapter 配置 |
| 不承诺 | 休眠、断电、进程崩溃后的恢复 | 高可用、自动迁移、多租户平台 |

玩家电脑示例可使用 Cloudflare Tunnel：它通过出站连接提供访问入口，房主不必具有可公网路由的 IP。[T7] 这是候选，不是指定必须依赖的供应商；远程可达性应从朋友的真实网络验证。

### Docker 构筑与运行（S3 新增约束）

全部构筑与运行在 Docker 容器内完成，与宿主机真实环境隔离，保证可迁移：开发、测试、生产使用同一镜像；宿主机不承担运行时依赖（除 Docker 本身）；数据目录、端口、会话秘密、媒体配置通过 compose 与 .env 注入，镜像内置默认值但不含真实密钥。交付物包含 Dockerfile、compose 编排与基础镜像版本锁定。未装入 Docker 的机器先完成环境准备，再进入 M1。

交付启动脚本与说明，不要求每位玩家安装开发工具。房主首次安装流程与日常启动分开；程序输出实际访问入口、房间邀请和基础状态。示例命令须在实现后实际验证，不能把本需求文档中的占位命令写成已可运行。

### 配置与秘密

建议配置项：监听端口、PUBLIC_BASE_URL、数据目录、会话秘密、语音开关、媒体服务地址与服务端密钥。提供 .env.example，但不能提交真实密钥；前端只得到短期、最小权限的媒体凭证。日志与数据库不放在静态网站公开目录。

请求层应有认证、允许来源校验、输入大小 / 类型限制和基本限流；公共文字允许活人白天自由发言，不等于无限字节或无限刷屏。文本按纯文本安全呈现，禁止把玩家昵称 / 消息直接当 HTML。[T5]

### 交付运行手册必须覆盖

实际支持的房主操作系统与版本；锁定的运行时 / 依赖；安装、启动、停止、入口失效排查；日志位置与删除 / 导出；第三方部署的数据目录与秘密注入；媒体关闭及失败提示。第一版不隐式承担购买服务器、域名或语音套餐的授权。

> **运维与普通管理员**  
> 用户仅确认局内不看身份，并未批准任意改状态、回滚、代行动或暂停面板。首版不能沿用旧脑暴里的这些建议来绕过规则；是否添加显式中止整局功能，应作为产品选项另行记录。

<!-- pagebreak -->

## 13  规则验收用例 A

以下是要实现的测试规范，不是已经执行的测试结果。用例输入固定板子、合法行动序列和预期输出；只涉及已确认条款的用例应当具有确定断言。

| ID | 输入 / 场景 | 预期 |
| --- | --- | --- |
| T-01 | 默认角色配置 | 13 个唯一席位；9 种身份，人数逐项一致。 |
| T-02 | 同凭证刷新 / 再加入 | 原席位原身份，不再抽牌。 |
| T-03 | 一阶段 2 名魂灵存活 | 共享上限 2；选 0、1、2 刀合法，3 刀拒绝。 |
| T-04 | 一阶段仅 1 名魂灵存活 | 上限 1，不继续保留开局 2 刀。 |
| T-05 | 死神 + 魂灵命中 3 个不同人，全死，开局魂灵 2 | 死神一阶段失技。 |
| T-06 | 同上但守护 / 还魂曲后仅 2 人真死 | 不触发失技。 |
| T-07 | 死神与魂灵多刀同一目标 | 攻击事件分开；失技人数按目标去重。 |
| T-08 | 死神一阶段已失技后转二阶段 | 不再受该标记禁用；联合上限仍为 2。 |
| T-09 | 守护目标受 1 刀 / 2 刀 | 分别为被挡存活 / 被挡一刀仍濒死，均算该目标守护成功。 |
| T-10 | 守护两个目标均受攻击 | 一阶段门先生触发牺牲；二阶段不牺牲。 |
| T-11 | 门先生先遭直接致命攻击，再双守成功 | 主死因为攻击；转阶段不回归。 |
| T-12 | 门先生仅因双守牺牲而死 | 转阶段时立即回归；不能重复回归。 |
| T-13 | 多攻击加牺牲事件 | 仅首个有效致死事件为主死因，后续事件不覆盖。 |
| T-14 | 守护同一对换顺序 / 同人连守三晚 | 连续限制仍生效。 |
| T-15 | 莱莱可先濒死后轮到刺杀 | 本夜仍能刺杀；一次性次数按合法使用处理。 |
| T-16 | 一阶段莱莱可用技能后到晨间 | 翻牌并失票；转二阶段恢复票权；不叠加未翻牌分支。 |
| T-17 | 二阶段未翻牌莱莱可 | 具备每晚一次刺杀分支。 |

涉及同一团队重复命中同目标、攻击批次内部具体排序的用例，按 R-48 定值编写确定断言（见 T-43）。

<!-- pagebreak -->

## 14  规则验收用例 B

| ID | 输入 / 场景 | 预期 |
| --- | --- | --- |
| T-18 | 濒死水妖一阶段行动 | 可救其他名单内角色，不能自救。 |
| T-19 | 水妖选择未濒死者 / 白天旧死者 | 拒绝且不消耗；不暴露被守护原因。 |
| T-20 | 一阶段还魂曲救濒死者 | 免于本夜死亡，不产生“死亡后复活”的被动链。 |
| T-21 | 水妖已用还魂曲，下一夜刷新 | 不恢复次数，也不重新获得一阶段名单。 |
| T-22 | 二阶段水妖夜间死亡 | 45 秒内可选自己以外已死者；次晨公告前回归。 |
| T-23 | 二阶段水妖白天被票死 | 不产生回归选择窗口。 |
| T-24 | 一阶段末夜水妖死亡，晨间转二阶段 | 不追溯触发回归。 |
| T-25 | 科研员夜间濒死 | 本夜技能仍用一阶段；死亡被救则不按已出局触发。 |
| T-26 | 科研员回归且原有一边已全灭 | 死神胜利的科研员死亡前置不成立；阶段保持二。 |
| T-27 | 人类与死神胜利同时成立 | 死神阵营胜，且只产生一个结果事件。 |
| T-28 | 仅丧亲者是存活死亡阵营成员，人类其他条件无冲突 | 人类获胜，不要求淘汰丧亲者。 |
| T-29 | 查丧亲者的魂灵 / 阵营 / 精确死神标签 | 否 / 是 / 否，不混用角色和阵营。 |
| T-30 | 开局死神 / 丧亲者已知身份响应 | 双方均不知道对方；各自只得规定魂灵知识。 |
| T-31 | 魂灵死亡后发消息 / 共同决策 | 读取允许，写及攻击操作拒绝。 |
| T-32 | 魂灵回归 | 恢复当前读写 / 决策资格，不重置使用记录。 |
| T-33 | 活人夜间发公屏 / 白天在获准阵营房发言 | 前者拒绝，后者允许。 |
| T-34 | 原例守 1、2，二者各只受一刀，水妖选 1 | 1 不在濒死名单，选择非法；门先生和 3 的死亡仍可按例验证。 |
| T-35 | 夜间中途恰好暂时满足胜负表达式 | 不提前结束；只在规定白天检查点判定。 |
| T-36 | 一阶段两名魂灵仍存活、死神未失技 | 最多 4 个总攻击名额；不是强制 4 人死亡。 |

### S3 裁定新增用例（T-37–T-50）

| ID | 输入 / 场景 | 预期 |
| --- | --- | --- |
| T-37 | 天理竞选平票 | 平票者重投一次；再次平票则本局无天理。 |
| T-38 | 普通票与天理票混投 | 以 0.5 为单位整数结票：普通票 2 单位、天理票 3 单位，比较正确。 |
| T-39 | 首夜死者 / 白天出局者 / 后续夜晚死者 | 前二者各获 60 秒遗言；后续夜晚死者无遗言；被还魂曲救回者无遗言。 |
| T-40 | 天理夜间死亡 / 白天出局 | 晨间公告后 / 遗言后 45 秒内可指定存活玩家继承；超时或指定无效则销毁、不重选。 |
| T-41 | 莱莱可翻牌禁投期间仍持天理 | 竞选与放逐均不能投票，票权冻结；二阶段恢复票权后同步恢复。 |
| T-42 | 团队方案版本更迭、重复确认、超时 | 新版本需重新全员确认；重复确认幂等；截止取最新全员确认版本；从未达成则空刀。 |
| T-43 | 同一团队对同一目标两刀，目标另有守护 | 两刀按 R-48 次序结算；守护只挡第一刀；第二刀致濒死；多刀计入名额。 |
| T-44 | 死神 / 魂灵 / 莱莱可自刀与自刺 | 合法；按濒死 / 死亡与翻牌规则正常结算。 |
| T-45 | 科研员死亡公告 | 报公告时点存活死神阵营人数（含丧亲者），整局限一次。 |
| T-46 | 科研员夜间死亡后同晨被水妖回归 | 阶段仍按死亡事件锁定转换为二阶段；门先生回归等效果照常。 |
| T-47 | 转二阶段时死神已死 / 魂灵全灭 | 已死则只读加入；魂灵全灭则不加入且此后不补拉；历史不含加入前消息。 |
| T-48 | 终局复盘 | 公开全部交流（含加入前阵营房历史）、行动日志、全身份、最终状态、胜负与时间线；无密钥凭证。 |
| T-49 | 变体配置（改人数 / 重复角色） | 正式模式拒绝；实验模式醒目提示并保存实验值。 |
| T-50 | 天理为莱莱可且处于禁投，其票为唯一决胜票 | 该票不生效，按剩余票数重新判断平票 / 出局结果。 |

全局不变量：已终局不再接受游戏行动；阶段不逆转；每次死亡主死因至多一个；同一死亡触发不得重复；不存在两个玩家共享同一席位；公共授权响应不含未公开角色；天理票权以 0.5 为单位整数结算。

<!-- pagebreak -->

## 15  异常、权限与真实环境验收

| 组别 | 必须覆盖 |
| --- | --- |
| 命令一致性 | 相同请求重发、不同请求 ID 重复确认、同玩家两个标签页、旧窗口延迟包、截止时竞态；只产生一份有效动作。 |
| 断线与刷新 | 身份、技能次数、个人日志、公共日志、阵营历史、确认状态与倒计时恢复；无自动暂停，无刷新补时。 |
| 越权反例 | 平民索要全身份、猜阵营房 ID、丧亲者订阅队聊、死神一阶段订阅队聊、死者伪造发言 / 攻击、局内提前请求终局复盘。 |
| 泄漏检查 | 网络载荷、错误、控制台、公开事件编号、公开时间变化、按钮变灰、成员列表；不能仅凭 UI 看不见就判通过。 |
| 语音 | 未授权麦克风、手动静音、死者尝试开麦、夜间发送、结束发言、重连复用旧凭证、媒体服务失败时文字继续。 |
| 部署与容量 | 同一 Docker 镜像部署到玩家电脑 / 第三方；从真实不同网络连接；至少 13 个脚本客户端完整日夜循环，记录实际资源与延迟，不凭显卡型号作结论。 |
| 浏览器 | Chromium 与 WebKit 自动化；真实桌面 Safari、真实手机 Safari 和目标 Chromium 设备上的核心操作与音频。 |

Playwright 的 WebKit 并不是品牌 Safari，模拟手机尺寸也不等同于真实手机和麦克风已经通过。因此自动化结果与真实设备验收必须分开列出。[T6]

### Codex 模拟对局的交付格式

提供可重复运行的脚本客户端和至少一条完整时间线：加入 → 发身份 → 多夜攻击与守护 → 白天投票 → 阶段转换 → 复活 → 胜负 → 复盘。无需 13 个大模型参局；正常路径测试不能替代越权反例。

报告必须包含：实际运行命令、代码版本、配置版本 / 快照、运行环境、用例总数、通过 / 失败 / 跳过数、失败动作序列与重现种子、证据路径、未测设备、未覆盖的 Q 条款。未运行就写“未运行”。

### 建议开发顺序

| 阶段 | 交付与退出条件 |
| --- | --- |
| M1 规则与数据 | 规则映射、Q 清单、纯引擎；已定规则单元测试通过。 |
| M2 文字闭环 | 加入 / 会话、窗口、确认、公共与阵营聊天、授权状态和日志；权限反例通过。 |
| M3 白天与复盘 | 按已批准白天附件完成竞选与投票；全局流程、胜负、回归、复盘与刷新通过。 |
| M4 语音与部署 | 可信公共发言控制、文字降级、两种部署；真实设备 / 真实网络报告。 |

不得通过删掉正确测试、修改已批准的预期结果来掩盖实现错误。实验模式配置与参数不得计入“全部通过”，必须在报告中单独标出。

<!-- pagebreak -->

## 附录 A  Q 裁定落地对照（S3 已清零）

Q-01–Q-08 已全部定值；下表与规则书附录 A 同编号，配置键与落地条款如下，编号仅作追踪，不再表示待定。

| 编号 | 配置 / 模块 | 定值结果 | 落地条款 |
| --- | --- | --- | --- |
| Q-01 | dayFlow、electionPolicy、votePolicy、voicePolicy | 白天时序、竞选、天理 1.5 票（0.5 单位整数）、平票、移交、遗言、票型、开麦子阶段 | R-41–R-46 |
| Q-02 | teamConfirmPolicy、attackOrder、duplicateTargetPolicy | unanimous_by_revision；确认后修改需重确认；多刀同目标允许；座位升序 + 来源优先级 | R-47、R-48 |
| Q-03 | targetPolicy、guardHistoryPolicy、passiveRepeatPolicy | 攻击可选自己与队友、死者不可选；未行动夜晚重置连续；公告限一次 | R-49 |
| Q-04 | researcherAnnouncementPolicy | 公告时点存活数，含丧亲者 | R-50 |
| Q-05 | deathVisionPolicy、transitionSnapshotPolicy | 二阶段降临者无名单；死亡事件锁定转换 | R-51 |
| Q-06 | historyPolicy、deathJoinPolicy | 转阶段时点一次判定；已死只读；不补拉；不含加入前历史 | R-52 |
| Q-07 | announcementProjection、replayDisclosurePolicy | 公告与复盘写复活；公开全部交流与行动日志；禁导出秘密 | R-53 |
| Q-08 | variantPreset、roleMultiplicity | 正式仅默认 13 人预设；变体走实验模式 | R-54 |

### 最终 11 项裁定的追踪

| 用户条目 | 落入规则 / 开发位置 |
| --- | --- |
| 1 魂灵按存活人数出刀 | R-30；§05；T-03 / T-04 |
| 2 一阶段失技不影响二阶段 | R-28 / R-29；§04；T-08 |
| 3 濒死可行动 | R-10；§05；T-15 / T-18 |
| 4、10 水妖不追溯 | R-23；§06；T-24 |
| 5 丧亲者查验 / 胜利例外 / 互知 | R-02 / R-24 / R-27 / R-31；§03；T-28–T-30 |
| 6 死神入房历史 | R-34、R-52；§07 |
| 7 科研员复活与胜利 | R-03；§06；T-26（上下文整理，非自动人类胜） |
| 8 首个有效主死因 | R-19 / R-32；§04；T-11–T-13 |
| 9 水妖白天无复活权 | R-23；§06；T-23 |
| 11 夜间公屏 / 白天队聊 | R-35；§07；T-33 |

<!-- pagebreak -->

## 附录 B  Codex 项目指令草案

可将以下文本放入项目 AGENTS.md。它是协作约束，不是声称 Codex 已执行或通过了任何测试。

```text
先读 theater_death_rulebook_v1.1.md 与本需求文档。
最新用户裁定优先；旧简报的冲突内容不再作为规则。
Q-01 至 Q-08 已全量定值（规则书第 09 章），不得用传统狼人杀惯例改写；新问题重新开号。
不要擅自新增角色技能、票权、平票、目标范围或复盘权限。
将玩法规则、实现约定和实验默认值明确分开。
先写纯规则和获批准的预期测试，再接网络与界面。
服务端拥有真实状态，客户端只能提交意图。
公共状态、个人历史、阵营历史都必须先授权后发送。
同一角色的多个连接不能增加行动或票数。
只做公共白天语音；夜间全体静音且公屏禁发。
允许阵营房死者只读，禁止任何写入和协作攻击。
一套程序支持电脑和第三方托管，不复制规则引擎。
全部构筑与运行在 Docker 容器内，与异构宿主机隔离、可迁移（见 §12）。
不擅自扩充服务器灾难恢复、录音、公开匹配和管理员改判。
所有规则改动须更新条款引用、测试及版本记录。
报告真实执行命令与结果；未运行、未覆盖必须标明。
不能修改正确测试预期来掩盖实现不符合规则。
```

### 附录 C  技术资料（不用于补充游戏规则）

以下官方资料核对日期为 2026-09-15。引用仅用于协议、权限与测试边界，不证明本项目已经实现、压测或完成安全审计。网址为技术参考代码文本。

[T1] Socket.IO — Delivery guarantees

```text
https://socket.io/docs/v4/delivery-guarantees/
```

[T2] Socket.IO — Client Socket instance

```text
https://socket.io/docs/v4/client-socket-instance/
```

[T3] Socket.IO — Connection state recovery

```text
https://socket.io/docs/v4/connection-state-recovery/
```

[T4] MDN — MediaDevices.getUserMedia

```text
https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia
```

[T5] OWASP — WebSocket Security Cheat Sheet

```text
https://cheatsheetseries.owasp.org/cheatsheets/WebSocket_Security_Cheat_Sheet.html
```

[T6] Playwright — Browsers

```text
https://playwright.dev/docs/browsers
```

[T7] Cloudflare — Tunnel

```text
https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/
```

[T8] Docker — Get started

```text
https://docs.docker.com/get-started/
```

## 附录：版本记录

### v1.2（2026-09-17）· 观战增补

- 用户裁定（阿真）：观战设计为“与一名玩家绑定的只读视角”，每名玩家最多绑定一名观众，昵称必填。
- §01 不承诺清单：移除“观战”通用条目，限定为仅支持“绑定玩家只读第二屏”形态（上帝视角仍不承诺）。
- §07 新增“旁观者”小节（可见范围 / 禁止项 / 会话与席位约束 / 计量提示）。
- 实现与测试：`server/session.ts`（会话 kind）、`server/rooms.ts`（`Room.spectators` + 1:1 绑定 + 玩家离开清理）、`server/app.ts`（`POST /api/rooms/:code/watch`、`POST /api/spectate/leave`、`GET /api/rooms/:code/members`、`resolveViewer` 分流；命令/聊天写/语音同步拒绝，视图/聊天读/复盘按绑定玩家）、`server/realtime.ts`（观战会话只入公共频道与绑定玩家个人频道）、前端入口与只读模式、语音旁听。
- 测试：`tests/spectator.test.ts` 7 例（含“观众视图与绑定玩家逐字段一致”“命令与发言拒绝”“复盘授权”“实时同流”）；E2E `07-spectator.spec.ts` 2 例；单测总数 194、E2E 16/16。

### v1.3（2026-09-17）· 房主踢人

- 用户裁定（阿真）：踢人仅未开局大厅期有效（对局中角色/胜负/身份不存在被移出场景）；被移出者**释放席位、可重新加入**（踢人 = 清位，不做拉黑）；房主**可单独移出观战者**（不限阶段）。
- 边界：房主不能移出自己（提示使用解散）；被移出者不占席位、不追加冷却或黑名单；被移出玩家可改以观战身份进入。
- 实现与测试：`server/rooms.ts`（`kickMember` / `kickSpectator` + 成员与观众清理共用私有方法）、`server/app.ts`（`POST /api/rooms/:code/kick`，房主校验；观战者移出时调用媒体服务移除参与者；`not_member` 文案统一为“你已不在该房间中”）、`voice/livekit.ts`（`removeParticipant`，幂等静默；观战者主动退出观战时同样调用）、前端大厅成员行/观战行“移出”按钮 + 确认弹窗、被移出者在轮询内回入口页并显示提示。
- 测试：`tests/server-api.test.ts` 踢人 7 例（移出与重进/权限/自我/开局后/连带/单踢观战者/目标校验）+ 语音参与者移除 2 例（移出与主动退出）、`tests/voice-livekit.test.ts` `removeParticipant` 幂等与异常 1 例；E2E `08-kick.spec.ts` 2 例；单测总数 204、E2E 18/18。

### v1.4（2026-09-17）· 实验模式板子编辑器

- 用户裁定（阿真）：入口页提供“傻瓜式”板子编辑器；**只允许调整各角色数量**（其余参数固定默认板值）；不提供浏览器本地保存（每次重编）；已创建房间的板子照旧落服务器数据库。
- 交互：入口页「自定义板子…」→ 编辑器（± 调节 9 个角色、分组小计与总人数、实时校验错误列表、有错禁用创建、恢复默认）→「用此板创建房间」→ 大厅实验模式横幅 + 按新人数开局。
- 实现：`web/src/board-editor.tsx` **直接复用 `rulesets/validate.ts` 与默认板**（前端与服务器同一份校验逻辑，不漂移）；创建时 `mode` 强制 `experimental`、`version` 标记 `custom`；服务端零改动（`POST /api/rooms` 既有校验与落盘路径）。
- 边界：神职 / 科研员 / 死神 / 丧亲者最多 1（与校验器一致）；平民 / 魂灵不限；未知人数组合能否正常对局由实验者自担——界面沿用 §14 “不得宣布任意人数 / 任意组合都可用”的明示义务。
- 测试：E2E `09-board-editor.spec.ts` 1 例（默认 13 人 → 改 10 人 → 制造非法看到错误与禁用 → 恢复 → 创建 → 大厅横幅与“满 10 人”文案）；单测总数 204 不变。

### v1.5（2026-09-18）· 终局退出

- 贡献提案（kiahir），用户（阿真）确认：**只放开终局后的退出**；对局进行中（夜/晨/昼）继续 409 拒绝——13 人板的胜负、失技阈值、遗言与票权都绑定 13 个席位，中途退出无规则依据（Q-01–Q-08 未覆盖该场景）。
- 终局语义：任何人（含房主）点「退出房间」都只**释放自己的席位**；房主退出**不解散**（其他成员照旧查看复盘）；**最后一名成员退出时房间销毁**（顺带解决终局房间常驻内存）。退出即清会话 cookie；房间已开局不能重新加入，等于放弃该局复盘访问——界面确认弹窗明示。
- 实现：`server/rooms.ts::leaveRoom`（守卫改为“非终局才拒绝”；终局分支 = 移除成员 + 连带观众 + 空房销毁双索引）、`server/app.ts`（`/api/view` 对局形态新增 `roomCode`）、`web/src/game.tsx`（终局卡片「查看复盘 / 退出房间」，观战者不显示后者）、`web/src/app.tsx`（`leaveRoom` 回调：清本地状态回入口页）。
- 测试：`tests/server-api.test.ts` 终局退出 3 例（成员退出且他人复盘保留 / 房主退出不解散 / 最后一人退出销毁房间），该文件 24→27 例；E2E `10-end-exit.spec.ts` 2 例（对局中退出被拒 409 / 终局退出回入口页且他人复盘保留）。单测总数 207；本次按测试策略只增量运行 `tests/server-api.test.ts`（容器内构建链全量跑）与 `specs/10-end-exit.spec.ts`（2/2 通过）。

### v1.6（2026-09-19）· 语音媒体服务替换为声网（实现约定变更）

- 动因：服务器使用 LiveKit Cloud 期间，中国大陆网络每次连接语音需十几秒（UDP 媒体探测被阻后等待超时回退中继）；媒体延迟可用但连接体验差，且跨境链路受运营商 QoS 影响不稳定。
- 变更：媒体服务由 LiveKit（自托管 / Cloud 双栖）替换为**声网 Agora 免费层**；R-43 的玩法语义与许可时段不变，实现改为「连麦鉴权 + 短期 token」——加入凭证为订阅角色（可听不可发）；轮到发言时服务端在状态推进时下发**发布凭证**（默认 TTL 10 分钟，覆盖最长 90 秒窗口），前端 `renewToken` 即时生效；收回时下发订阅凭证即时降权，发布权限到期由声网侧自动兜底。踢人（观战者）与终局关房改走声网频道管理 REST（`kicking-rule`：一次性踢出、可立即重进，与“踢人不拉黑”语义一致）。
- 实现：`voice/agora.ts`（新增，替换 `voice/livekit.ts`）、`server/rooms.ts`（权限变化时签发 token 并随 `voice_permission` 推送）、`server/app.ts`（`/api/voice/token`、`/api/voice/sync` 改为 uid 体系；观战者 uid 从 1000 起稳定分配）、`server/realtime.ts`（推送载荷 `{ permission, token? }`）、`web/src/voice.tsx`（`agora-rtc-sdk-ng` 重写）、依赖与部署配置（compose 移除 livekit 服务；`.env` 改为 `AGORA_APP_ID` / `AGORA_APP_CERTIFICATE`；控制台须开启「连麦鉴权」）。
- 测试：单测 `tests/voice-agora.test.ts` 6 例（token 格式与 TTL、REST 踢人/关房与鉴权头）替换原 LiveKit 适配测试；`voice-api` / `spectator` / `server-api` 适配新接口；E2E 语音断言降级为“频道在线（声网 REST）+ 界面文案 + 无麦克风错误”（声网不提供发流/权限状态查询）。已于 2026-09-19 完成声网凭据配置与真实云联调（E2E 语音 3 例全过：加入频道、发言授权/收回、夜间禁麦）。

### v1.7（2026-09-19）· 天理夜死移交时机对齐 + 规则 2.0 命名预设（玩法变更；贡献提案 syhneversigh，用户（阿真）确认）

- **天理夜间死亡移交时机**：由“统一在白天流程末尾（票型公示后）固定步办理”改为“晨间公告（及首夜遗言，若同日在先）后立即办理，办结后回到发言轮起”；白天出局的移交不变（遗言后、结算前）。实现对齐 R-46 与 T-40 字面（两者原文即“晨间公告后 / 遗言后”），原“固定步”实现约定取消。
- **规则 2.0 命名预设（`THEATER_DEATH_13_V2`）**：新增可选命名预设，正式模式允许默认 1.1 与 2.0 两个命名预设（R-54 相应扩展）；差异点：V2-01 结算后立即判胜负（成立即终局，跳过遗言与移交）；V2-02 首日竞选先于晨间公告（首夜存活名单 `eligibleAtStart` 判定公告前白天权限）；V2-03 发言 120 秒 + 15 秒不开麦准备窗口（`START_SPEECH` 提前开始）；V2-04 提案兜底最后合法草稿（含空刀，覆盖 R-47“从未达成视为空行动”）。默认 1.1 预设行为不变；完整增补见 `docs/rules-v2.md`。
- 实现：`engine/day.ts`（移交时机、v2 立即终局与公告前竞选）、`engine/proposal.ts`（`resolvedProposal`）、`engine/night.ts`（`eligibleAtStart`）、`engine/types.ts`、`rulesets/theater-death-13-v2.ts`（新增）、`rulesets/validate.ts`、`server/day-driver.ts`（准备窗口、截止即拒、代际守卫）、`server/night-driver.ts`（`strictWindows`、v2 首日流程）、`server/commands.ts` / `server/windows.ts` / `server/queued-clock.ts`、`visibility/review.ts`（`messageId` 透传）。
- 测试：`engine-day`（T-40 新预期）、`day-driver`、`night-driver`（`ProposalView.effective`）、`deadline-queue`（4 例）、`window-instances`（3 例）；本次按测试策略增量运行 18 文件 193 例全过 + 类型检查（容器内构建链全量兜底）。

### v1.8（2026-09-19）· 账号体系与 v2 服务端 / 前端体系准入（贡献提案 syhneversigh，用户（阿真）确认；媒体层改造为声网）

- **准入范围**（PR #3 选定部分照抄，语音统一为声网，v1 入口与既有玩法不变）：
  - **账号体系**：数字 UID 注册 / 登录（10000001 起、字符串传输、删除不复用）、scrypt 密码（8–16 码点，兼容迁移旧哈希）、口令会话（7 天）、改名 / 改密 / 停用 / 永久删除、注册开关持久化、管理后台（`ADMIN_PASSWORD` ≥16 位 + `td_admin_v2` cookie + `admin_actions` 审计 + 限流）、schema 4 迁移（保留账号 ID、哈希与有效会话）。
  - **房间与席位模型（v2）**：StableRoom（房间与单局解耦）＋ RoomDirectory / Access 租约（sessionId + epoch）＋ 接管（旧设备断权）＋ 暂离对局（局中离开保留席位，同账号可恢复）＋ 在线状态（心跳 10s / 超时 20s / 断线宽限 15s）＋ 空房策略（无正式成员 5 分钟 TTL、复盘期最后一名正式成员离开即销毁、局中超时按 aborted 落审计）＋ 房主继任。
  - **服务端 v2**：`server/v2`（`/api/v2` 前缀、契约 2.1/2.2、回执幂等 receipts + 指纹、窗口实例 strictWindows、公开 / 个人 / 第二屏三级视图、任务窗口 capabilities / targets、维护清扫）。
  - **web-v2 前端**：独立构建（`vite.v2.config.ts`、`npm run build:web:v2`）：大厅 / 昼夜行动 / 情报 / 公屏 / 观战 / 终局复盘与账号管理；头像 sharp 处理。
  - **语音**：v2 语音层由 LiveKit 改造为声网（与 v1 同一 `voice/agora.ts`；uid 由媒体身份稳定映射；发布权编码在短期 token，前端 `renewToken` 即时生效；`/api/v2/voice/sync` 保留为轻量对齐——关房与回收失效租约）。旧 LiveKit 自托管残留（`deploy/livekit*.yaml`、`render-livekit-config.sh` 等）不再入库。
  - **契约与文档**：`contracts/*`、`docs/*`（openapi v2.2、client-contract 2.1/2.2、frontend-v2-*、rules-v2 增补）、E2E `specs-v2`（16 例）+ `helpers-v2`、`tests/fixtures/contract-2.1`（30 份快照）。
- **边界**：v1 入口（3000）与既有玩法保持不变；v2 为并行体系（独立进程 `server/v2/index.ts`、独立数据目录 `data-v2`）；媒体服务全仓库统一为声网。
- 测试：84 文件 517 例全过（含 v2 全套与 v1 回归）+ 根 / 前端 v2 双类型检查 + `build:web:v2` 构建通过。本次为无先例大集成，按全量回归执行（增量测试策略之外的临时例外）；容器内构建链亦全量兜底。

### v1.9（2026-09-19）· 文档一致性整理（无玩法变更；贡献提案 kiahir，用户（阿真）确认）

- **范围**：全仓库 24 个 md 逐项核对（对照实际代码、compose 与测试静态清点），修正上游 v1.6–v1.8 合并后遗留的四类不一致。**不涉及玩法、接口或数据结构变更。**
- **媒体服务残留（LiveKit → 声网）**：
  - 文档：`docs/frontend-v2-voice.md` 的"本地验证 / 公网同机部署模板"整段由 LiveKit 自托管改写为声网托管（本地无需媒体容器与 7881/7882 端口；公网先开启控制台「连麦鉴权」并配 `AGORA_*`，服务器无需媒体入站端口）；`docs/backend-v2-api.md` 语音段（原"30 秒加入凭证 / 发布权限由服务端同步 / LiveKit 签名 Webhook"纠正为加入凭证 30 分钟、发布凭证 10 分钟、webhook 未启用）；`docs/backend-v2-runbook.md` 的公网部署提法；§07 旁观者的媒体计费表述（原 "LiveKit Cloud 免费层"）。
  - 代码与配置：`deploy/frontend-local.ps1` 去掉已不存在的 `livekit` 服务引用（**`-Voice on` 因此恢复可用**）；`deploy/compose.frontend-local.yml` 与 `deploy/compose.v2.release.yml` 删除失效的 `LIVEKIT_API_KEY` / `LIVEKIT_API_SECRET` / `VOICE_SERVICE_URL` / `VOICE_ADMIN_URL`（仓库内无任何代码读取）。
  - **仍未处理（需另行定案）**：`docs/openapi-v2.2.json` 的 `/voice/webhook`（`livekitSignature`）与 `server/v2/app.ts` 中未被注入的 `verifyWebhook` 路由（声网方案下不会被注册，属未启用历史遗留）；`deploy/docker-local-common.ps1` 的错误文案指向未入库的 `recover-docker-sockets.ps1`。
- **版本号与计数**：本文档表头 v1.5 → **v1.9**（正文此前已列到 v1.8）、整理日期更新；§01「不承诺」移除已交付的"账号体系"并注明 v1.8 准入；`AGENTS.md` 与 `PROGRESS.md` 的单测计数 207 / 208（18 文件）→ **520 / 84 文件**（2026-09-19 全量实测：容器构建链 84 文件 520 例全过；与 v1.8 记录的 517 例之差 = 其后 `7522284` 为 `tests/knowledge.test.ts` / `voice-policy.test.ts` 新增的 3 例）；`PROGRESS.md` 状态表补「终局退出 v1.5」行、"接续指引"版本号更新、文件清单按当前结构重写、清除"livekit 配置待删"的过期待办。
- **失效链接**：`docs/client-contract-2.1.md`、`client-contract-2.1-runbook.md`、`client-contract-2.1-verification.md`、`client-contract-2.1-examples.md`、`frontend-v2-voice.md`、`rules-v2.md`、`backend-v2-api.md` 中指向未入库文档（2.1 验收 / 计划 / 进度、`openapi-v2.1.json`、`frontend-v2-handoff.md`、`backend-v2-plan.md` / `backend-v2-progress.md`）的链接，改为"贡献者本地记录（未入库）"说明，schema 指向当前 `docs/openapi-v2.2.json`。
- **规则正文**：R-54 正文（`theater_death_rulebook_v1.1.md` 与 `docs/rules-v2-full.md`）由"正式模式仅提供默认 13 人命名预设"同步为"提供 1.1 与 2.0 两个命名预设"，与 v1.7 的扩展一致（原表述未随之更新）。
- **保留不动**：`M4_ACCEPTANCE_REPORT.md` 为 2026-09-16 收官时点快照（187 单测 / E2E 14/14 / 媒体为 LiveKit Cloud），仅加"历史快照"抬头注记，正文数据不改。
- **测试**：本次为纯文档整理 + 脚本/配置引用清理，**未运行任何测试**（符合测试策略：纯文档 / 只读研究不必跑测试）；`deploy/frontend-local.ps1 -Voice on` 需在装有 Docker 的机器上实测确认。

### v2.0.1-beta（2026-09-20）· 房间解散任意阶段生效 + 遗弃房间 24 小时回收（贡献提案 kiahir，用户（阿真）确认）

- **版本号说明**：本版起本文档的版本名改用分支名 **`v2.0.1-beta`**（与推送分支 `2.0.1-beta` 一致），后续沿用该序列。注意它与**规则版本 2.0**（`docs/rules-v2-full.md`）、**客户端契约 2.1/2.2** 是三套独立编号，不要混用。**文件名保留基线名 `theater_death_development_requirements_v1.1.md` 不变**（改名会断开外部链接与全仓引用），即「文件名=基线版本、内容=累积版本」。

- **v2 解散改为任意阶段立即生效**：`server/v2/governance.ts::dissolve` 去掉 `phase !== 'lobby'` → 409 `lobby_required` 的限制，仅保留「必须是房主」（非房主 403 `not_host`）。`dispose()` 增加幂等守卫，并在处置**未产生胜负**的对局时按 `aborted` 记入审计（与空房到期同一口径，`finishMatch` 自身幂等）；已终局的保持 `completed`。权限快照 `capabilities.room.dissolve` 只取决于是否房主，不再出现 `lobby_required`。
- **房主资格与继任（用户（阿真）确认：只看在线与否）**：接任/接手房主只要求「正式成员 + 在线且有连接 + 会话有效（手动移交时目标还需在线）」，**不限制游戏内生死**——**已出局的正式成员同样可以继任房主**，因此也持有解散 / 踢人 / 移交房主的权限。这是刻意的产品语义（房主是房间管理角色、不是玩法角色；房间管理权不必随出局转移）；审议后决定**不加"死者不接任"规则**。完全没有任何在线正式成员时 `hostMemberId` 为 `null`，房主类操作在快照里对所有人隐藏（`capabilities.room` 的 host 相关项不可用），仍可由成员离开触发空置策略关闭房间。
- **v2 房主离开语义**：房主在**大厅**退出即解散整房（响应 `{left:true, dissolved:true, seatRetained:false}`）；房主在**对局进行中**退出是「暂离」（席位与计时保留，房主由其他在线正式成员继任，响应 `dissolved:false`）；房主在**复盘**退出是**普通离开**（房间保留、其他人继续看复盘、房主同样继任）。非房主一律按普通离开。实现为新增 `RoomGovernance.leave`，HTTP `/leave` 改走它；`RoomDirectory.leave` 保留为底层原语（不处理房主解散语义，避免测试脚手架与领域层混淆）。空置导致房间被关闭时回执的 `seatRetained` 归 `false`（沿用原契约）。
- **v2 全员离线回收（原 F1）**：新增 `OFFLINE_ROOM_TTL_MS = 24 小时`（`server/v2/empty-rooms.ts`）与 `StableRoom.allOfflineSince`。有正式成员但**全员离线**满 24 小时即回收房间（未终局按 aborted 记账）；只要一人回到在线即取消计时；观众不能延长。此前「离线与已死的正式成员都算非空」叠加「解散需要在线房主」，使被遗弃房间既无法解散也不会被回收，可占满 100 房上限并让新建房间返回 `503 room_capacity`（现由本策略兜底）。
- **v1 无活动回收（原 F2）**：v1 没有在线状态（无状态 cookie + 内存房间），因此采用「房间无活动」口径——`Room.lastActivityAt`（任何房间 HTTP 请求经 `resolveRoomMember`/`resolveViewer` 刷新，实时握手在 `server/realtime.ts` 刷新）与 `RoomRegistry.sweepInactive(now, ttl)`；`server/legacy-index.ts` 每 60 秒扫描一次，超过 `INACTIVE_ROOM_TTL_MS = 24 小时` 无活动即销毁（成员下次请求得到 404 → 前端回入口页）。v2 稳定房间（`queueOwner` 非空）不在 v1 扫描范围内；`disposeRoom` 现在同时释放 `room.driver` 引用。
- **前端文案与交互**：`web-v2` 的 `roomExitPresentation` 按新语义改写退出说明（房主大厅/复盘退出=解散，对局中=暂离）；`lobby.tsx` 解散确认弹窗按阶段给出更强提示（对局中「立即终止本局并按中止记录」）；`review/page.tsx` 处理 `dissolved` 响应并提示「房间已解散」。解散入口沿用既有 `caps.dissolve.allowed` 门控（对局中的入口在「房间管理」内，复盘页同）。
- **测试**：`tests/room-governance.test.ts`（任意阶段解散 + 非房主拒绝 + 房主离开的三阶段语义）、`tests/empty-rooms.test.ts` 与 `tests/v2-maintenance.test.ts`（离线 TTL 前保留 / 满期回收 / 审计保持 completed / 销毁后无残留定时器）、`tests/server-api.test.ts` 新增 3 例（HTTP 请求刷新活动时间、实时握手算活动、对局中回收释放驱动）、`tests/room-operation-api.test.ts`（复盘阶段房主最后离开=房间关闭并重放原回执）、`tests/empty-rooms.test.ts` 的 `forget` 兜底用例。全量：84 文件 526 例。
- **兜底硬化（F2）**：`EmptyRooms.forget(roomId)` 取消并清除 `pending` / `offlinePending` 中的定时器，由 `RoomDirectory` 的 `removed` 钩子调用——这样即使将来出现"不经 `observe` 的销毁路径"（如管理端直接解散空房），也不会留下永不执行的条目。当前该组合不可达（能解散 ⇒ 有在线房主 ⇒ 两个定时器早已取消），属防御性加固；`tests/empty-rooms.test.ts` 直接覆盖 `forget` 的契约。
- **E2E 补充（F4）**：新增 v1 用例 `e2e/specs/11-dissolve.spec.ts`（大厅期房主「解散房间」→ 房主回入口页、其余成员会话失效 404、房间码失效无法重进）；v2 在 `e2e/specs-v2/02-rooms.spec.ts` 新增「房主在大厅用『离开房间』退出即解散整房，其余成员收到 `control{dissolved}` 并回首页」。v1 入口静态清点为 18 例（chromium 18 + webkit 4 = 22 次运行），v2 入口 16 个 spec / 42 例。
- **契约夹具**：核对确认夹具里**没有**「房主在非大厅阶段」的快照，因此不存在与本次变更冲突的 `dissolve` 取值——`lobby-formal-full.json`（房主·大厅）仍是 `dissolve=true`，其余夹具的 `dissolve` 都是 `not_host`（仍然成立）；夹具文本里出现的 `lobby_required` 属于 ready / kickFormal 等其他能力。夹具只做 schema 校验（`tests/contract-openapi.test.ts`），无需重新导出即与实现一致；若要补「房主在 playing/review 可解散」的样例，再用 `EXPORT_CONTRACT_FIXTURES` 导出。
- **两入口差异（有意保留）**：v1 的对局中 `/leave` 仍 409 拒绝（v1 无「暂离」概念），v2 的对局中离开是暂离；v1 的复盘（`phase='ended'`）里房主退出仍只释放自己席位，而 v2 复盘里房主退出即解散。如需把这两条也统一，属后续独立变更。
